@model Task<PagedList<Product>>

@{
    ViewBag.Title = "Shop";
}

<h1>Our products</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Result[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Result[0].Manufacturer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Result[0].Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Result[0].Category)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Result[0].Price)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Result)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Manufacturer)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Category)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Price)
                </td>
                <td>
                    <a class="btn btn-primary" asp-controller="Shop" asp-action="Details"
                       asp-route-id="@item.Id">Details</a>
                    <a class="btn btn-primary" asp-controller="ShoppingCart" asp-action="AddToShoppingCart" asp-route-productId="@item.Id"><i class="fas fa-shopping-basket"></i></a>
                </td>
            </tr>
        }
    </tbody>
</table>
